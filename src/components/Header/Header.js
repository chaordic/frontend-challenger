import React from "react";

import styles from "./Header.scss";

export const Header = () => (
  <header className={styles.header}>
    <div className={styles.container}>Tratamento de entregas</div>
  </header>
);

export default Header;
